/**
 * Domain classes used for communicating with the frontend. The service layer produces these and the frontend layer displays them.
 * They are to be serialized via json.
 * Note that I chose not to use the exact same model as the Abnamro API. The service layer transforms the Abnamro model into this extra indirection layer.
 */
package nl.brachio.abnamro.model;